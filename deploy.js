// This is the deployment of script 
// Deployed to Solana -> we need keypair to pay for that 

const programAuthorityKeyfile = `deploy/programautority-keypair.json`; 

(async()=> {

    if (!fs.existsSync(programAuthorityKeypairFile)) {
        // create it 
        // use to deploy 
    } else {
        // exists -> deploy 

    }
})